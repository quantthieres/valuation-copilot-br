# ─── Market data providers ────────────────────────────────────────────────────
# BRAPI_TOKEN is optional for local development.
# If not set (or if the provider fails), the app falls back to mock price data.
# Get a free token at https://brapi.dev
BRAPI_TOKEN=
